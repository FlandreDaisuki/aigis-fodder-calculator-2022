export const mismatchedFairy = Object.freeze({
  name: 'mismatched-fairy',
  exp: 500,
});

export const matchedFairy = Object.freeze({
  name: 'matched-fairy',
  exp: 1000,
});

export const whiteBucket = Object.freeze({
  name: 'white-bucket',
  exp: 5000,
});

export const blackBucket = Object.freeze({
  name: 'black-bucket',
  exp: 40000,
});

export const microGoldSprite = Object.freeze({
  name: 'micro-gold-sprite',
  exp: 1750,
  rarity: 4,
});

export const goldSpriteOrLootSprite = Object.freeze({
  name: 'gold-sprite-or-loot-sprite',
  exp: 18000,
});

export const platinumSprite = Object.freeze({
  name: 'platinum-sprite',
  exp: 19000,
  rarity: 5,
});

export const blackSprite = Object.freeze({
  name: 'black-sprite',
  exp: 20000,
  rarity: 6,
});

export const fodderSprite = Object.freeze({
  name: 'fodder-sprite',
  exp: 10000,
});

export default Object.freeze({
  mismatchedFairy,
  matchedFairy,
  whiteBucket,
  blackBucket,
  microGoldSprite,
  goldSpriteOrLootSprite,
  platinumSprite,
  blackSprite,
  fodderSprite,
});
